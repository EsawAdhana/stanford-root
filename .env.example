# App (Next.js) – copy to .env.local and fill in. Never commit .env.local.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Feedback → email (optional). When set, new feedback is emailed to you.
# Get API key: https://resend.com/api-keys (free tier: 100 emails/day)
# For "from" you can use onboarding@resend.dev in dev, or verify your domain in production.
RESEND_API_KEY=re_xxxx
FEEDBACK_EMAIL_TO=your@email.com
# RESEND_FROM_EMAIL=Stanford Root <onboarding@resend.dev>

# Scripts only (run with env vars, do not put in .env.local):
# SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... node scripts/upload-courses.js
# SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... node scripts/upload-evaluations.js
# ALGOLIA_APP_ID=... ALGOLIA_API_KEY=... npx ts-node scripts/fetch-courses.ts
