node_modules
.next
out
.DS_Store
*.log
.env
.env.local
.env*.local
.tmp-pages

# Build artifacts
tsconfig.tsbuildinfo

# Scraper temp files
.eval-scrape-progress.json
.eval-backfill-progress.json
.eval-enrich-progress.json
.auth.json
.evaluationkit-profile/
.sections-scrape-progress.json
.final-exam-scrape-progress.json

# Generated per-course evaluation files
public/data/evals/

# Checkpoint only — light.json / full.json are committed so prod can serve them
data/catalog/full.partial.json

# Academic deliverables (proposal is internal-only)
PROPOSAL.md

# LaTeX build outputs (source: report.tex)
report.aux
report.fls
report.fdb_latexmk
report.log
report.out
report.pdf
report.synctex.gz

# Production tooling
CLAUDE.md
.env.example
public/google*.html
public/robots.txt
.vercel
.env*
.eval-search-cache/

# Agent and audit scratch: screenshots, one-off scripts, SDK install reports and
# Supabase CLI state (which holds a pooler connection string). None of it is part
# of the app, and the CLI state should never be committed.
.audit-ui/
tmp/
supabase/.temp/
humanbehavior-install-report.md
.claude/
.lavish/
.next-claude/
